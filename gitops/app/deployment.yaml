apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops-platform
  namespace: devops-platform
spec:
  replicas: 4
  selector:
    matchLabels:
      app: devops-platform
  template:
    metadata:
      labels:
        app: devops-platform
    spec:
      containers:
        - name: devops-platform
          image: agarciafer/devops-platform:v4
          ports:
            - containerPort: 5000
          env:
            - name: APP_VERSION
              value: "v3.0.0"
            - name: ENVIRONMENT
              value: "dev"  
          
